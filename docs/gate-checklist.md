# gate-checklist.md — 迭代 6 · 切片 6.4 放行清单

> 当前阶段：迭代 6 · 切片 6.4（聊天式 Web 界面）。放行决定：通过 / 重试 / 降级 / 停止。

## 必过项

- [x] 对话式布局：消息气泡 + 顶部工作区 + 底部输入框
- [x] SSE 流式渲染进 agent 气泡，工具步骤着色
- [x] `/run` `/events` 行为不变（不回归）
- [x] `pytest -q` 免 key 全绿

## 证据位置

- 测试与冒烟：见 `docs/AGENT_LOG.md` 迭代 6 切片 6.4 条目
- 代码：`agent/web.py`

## 退出决定

- 通过 → 迭代 6 优化完成
- 重试 → 补齐缺失项后复查
- 停止 → 记录原因
