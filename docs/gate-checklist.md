# gate-checklist.md — 迭代 9 · 修复切片放行清单

> 当前阶段：迭代 9 · 修复切片（/skill 命令与技能多选）。放行决定：通过 / 重试 / 降级 / 停止。

## 必过项

- [x] `/` 弹层仅 `/skill`；选后同弹层列全部技能，点击累积多选、再点取消
- [x] 技能栏点选累积（`/skill a,b`）不覆盖；`&skill=a,b` 多技能装载
- [x] `pytest -q`（146）、`node --check`、无头垫片、真实服务冒烟

## 证据位置

- 检查证据：见 `docs/AGENT_LOG.md` 迭代 9 修复切片条目
- 代码：`agent/web.py`（前端命令浮层与选择逻辑）

## 退出决定

- 通过 → 迭代 9 修复完成
- 重试 → 补齐缺失项后复查
- 停止 → 记录原因
