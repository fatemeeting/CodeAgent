# gate-checklist.md — 迭代 6 v2 · 切片 6.6 放行清单

> 当前阶段：迭代 6 v2 · 切片 6.6（工作区管理器 + 视觉系统）。放行决定：通过 / 重试 / 降级 / 停止。

## 必过项

- [x] 未选工作区：只显示整页工作区管理器，其余内容隐藏
- [x] 管理器三要素：选择（原生对话框/手动输入/最近列表）、确认（显式按钮 + `/tree` 校验）、管理（最近列表 localStorage + 顶栏 🔄 切换）
- [x] `GET /tree` 返回目录条目 / 不存在报错
- [x] 视觉系统：暖白背景 + 橙色强调 + 8px 间距
- [x] 既有端点不回归；`pytest -q` 全绿

## 证据位置

- 测试与冒烟：见 `docs/AGENT_LOG.md` 迭代 6 v2 切片 6.6 条目
- 代码：`agent/web.py`、`tests/test_web.py`

## 退出决定

- 通过 → 切片 6.7（Agent Window）
- 重试 → 补齐缺失项后复查
- 停止 → 记录原因
