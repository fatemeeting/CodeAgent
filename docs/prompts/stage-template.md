# stage-template.md — 阶段任务提示模板

> 每个阶段开始时，按此六项组织给 AI 的任务提示（PE 六项）。

## 1. 角色
你在本项目的职责：________

## 2. 任务
当前阶段只完成一个目标：________

## 3. 资料
需要读取的文件：________

## 4. 约束
不能改 / 不能引入：________

## 5. 输出
先计划还是直接改文件：________

## 6. 验收
通过哪些检查（见 CHECKLIST.md）：________

## 7. 切片收尾清单（每个切片完成后逐项勾选，防止遗漏）

- [ ] 代码已实现 + 单测全绿（`pytest -q`）
- [ ] `CHECKLIST.md` 增该切片的验收项
- [ ] `context-pack.md` 刷新到「当前切片」；整个阶段完成后刷新到「完成」态
- [ ] `gate-checklist.md` 更新为当前切片并勾选
- [ ] `AGENT_LOG.md` 记录证据 + 放行决定
- [ ] `context-snapshot.md` 更新已完成事实
- [ ] 提交信息含阶段/迭代名（`阶段N` / `迭代N切片X.Y`）
